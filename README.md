# bootstrap-oembed
A super-lightweight WordPress plugin which modifies the oEmbed filters to add bootstrap elements and styling to default WordPress embeds.

Currently only compatible with 16:9 YouTube videos.